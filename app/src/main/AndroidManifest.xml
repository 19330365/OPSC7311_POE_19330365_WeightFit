<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tillster.opsc7311_poe_19330365_weightfit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DietaryTips"></activity>
        <activity android:name=".DiaryLog" />
        <activity android:name=".WorkoutRoutines" />
        <activity android:name=".Home" />
        <activity android:name=".BodyGoal" />
        <activity android:name=".BodyType" />
        <activity android:name=".SetGoalType" />
        <activity android:name=".NewUser" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>